# 📘 Инструкция по установке и использованию Legal CMS-MD

**Для начинающих пользователей**

---

## 🎯 Что это за программа?

**Legal CMS-MD** - это программа для юристов, которая помогает:
- Вести учёт судебных дел
- Хранить все документы в одном месте
- Автоматически распознавать текст в документах (OCR)
- Следить за сроками и судебными заседаниями
- Искать информацию по всем делам
- Создавать отчёты

---

## 📋 Что нужно установить ПЕРЕД началом?

### 1. PostgreSQL (база данных)

**Что это?** Программа для хранения всех ваших данных (дел, документов, персон).

**Как установить на MX-Linux:**

```bash
# Шаг 1: Открыть терминал (Ctrl+Alt+T)

# Шаг 2: Установить PostgreSQL
sudo apt update
sudo apt install postgresql postgresql-contrib

# Шаг 3: Проверить что установилось
postgres --version
# Должно показать: postgres (PostgreSQL) 15.x или выше
```

### 2. Python 3.11 или новее

**Что это?** Язык программирования, на котором работает сервер.

**Как проверить версию:**

```bash
# В терминале
python3 --version
# Должно показать: Python 3.11.x или выше
```

**Если версия старая, установить новую:**

```bash
sudo apt update
sudo apt install python3.11 python3.11-venv python3-pip
```

### 3. Node.js и npm

**Что это?** Нужно для работы веб-интерфейса.

**Как установить:**

```bash
# Установить Node.js 18 или новее
sudo apt update
sudo apt install nodejs npm

# Проверить версию
node --version  # Должно быть v18.x или выше
npm --version   # Должно быть 9.x или выше
```

### 4. Ollama (для AI функций)

**Что это?** Программа для распознавания текста в документах (OCR) и умного поиска.

**Как установить:**

```bash
# Скачать и установить Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Проверить что установилось
ollama --version
```

---

## 🚀 УСТАНОВКА ПРОЕКТА

### ШАГ 1: Подготовить базу данных PostgreSQL

**Откройте терминал и выполните по порядку:**

```bash
# 1. Войти в PostgreSQL от имени администратора
sudo -u postgres psql

# 2. Создать базу данных (введите в psql)
CREATE DATABASE legal_cms;

# 3. Создать пользователя (ЗАМЕНИТЕ 'ваш_пароль' на реальный пароль!)
CREATE USER legal_cms_user WITH PASSWORD 'ваш_пароль';

# 4. Дать права пользователю
GRANT ALL PRIVILEGES ON DATABASE legal_cms TO legal_cms_user;

# 5. Выйти из psql
\q
```

**💡 ВАЖНО:** Запомните или запишите пароль, который вы придумали!

---

### ШАГ 2: Настроить Backend (сервер)

**1. Перейти в папку проекта:**

```bash
cd "Z:\FQ\Документооборот\Jurist\backend"
```

**2. Создать виртуальное окружение Python:**

```bash
# Создать окружение
python3 -m venv venv

# Активировать его
source venv/bin/activate

# После активации в начале строки появится (venv)
```

**3. Установить все нужные программы:**

```bash
pip install -r requirements.txt
```

**Это займёт 2-3 минуты. Дождитесь окончания!**

**4. Создать файл настроек (.env):**

```bash
# Скопировать пример
cp .env.example .env

# Открыть для редактирования
nano .env
```

**5. Изменить настройки в файле .env:**

Найдите строку `DATABASE_URL` и измените пароль:

```env
# БЫЛО:
DATABASE_URL=postgresql+asyncpg://legal_cms_user:PASSWORD@localhost/legal_cms

# СТАЛО (замените ваш_пароль на тот, что придумали в ШАГ 1):
DATABASE_URL=postgresql+asyncpg://legal_cms_user:ваш_пароль@localhost/legal_cms
```

Найдите строку `SECRET_KEY` и сгенерируйте новый ключ:

```bash
# В другом окне терминала выполните:
openssl rand -hex 32

# Скопируйте результат (например: a3f2b1c4d5e6...)
```

Вставьте этот ключ в .env:

```env
SECRET_KEY=a3f2b1c4d5e6...  # Ваш сгенерированный ключ
```

**Сохраните файл:**
- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить)
- Нажмите `Ctrl+X` (выйти)

**6. Создать таблицы в базе данных:**

```bash
# Убедитесь что (venv) активировано!
# Применить миграции
alembic upgrade head
```

**Вы должны увидеть:**
```
INFO  [alembic.runtime.migration] Running upgrade  -> 20251209_001, Initial migration
```

**7. Создать первого пользователя (администратора):**

```bash
# Создать файл
nano create_admin.py
```

**Вставьте этот код:**

```python
import asyncio
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from sqlalchemy.orm import sessionmaker
from app.models.user import User
from app.utils.security import get_password_hash
from app.config import settings

async def create_admin():
    engine = create_async_engine(settings.DATABASE_URL, echo=True)
    async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

    async with async_session() as session:
        admin = User(
            username="admin",
            email="admin@example.com",
            full_name="Администратор",
            hashed_password=get_password_hash("admin123"),
            role="admin",
            is_active=True
        )
        session.add(admin)
        await session.commit()
        print("✅ Администратор создан!")
        print("Логин: admin")
        print("Пароль: admin123")

asyncio.run(create_admin())
```

**Сохраните файл** (Ctrl+O, Enter, Ctrl+X)

**Запустите скрипт:**

```bash
python create_admin.py
```

**Вы должны увидеть:**
```
✅ Администратор создан!
Логин: admin
Пароль: admin123
```

**8. Создать папки для файлов:**

```bash
# Вернуться в корень проекта
cd ..

# Создать папки
mkdir -p storage/documents
mkdir -p storage/templates
mkdir -p storage/legal_acts
mkdir -p backups
```

---

### ШАГ 3: Настроить Frontend (веб-интерфейс)

**1. Перейти в папку frontend:**

```bash
cd "Z:\FQ\Документооборот\Jurist\frontend"
```

**2. Установить зависимости:**

```bash
npm install
```

**Это займёт 3-5 минут. Дождитесь окончания!**

**3. Создать файл настроек:**

```bash
# Скопировать пример
cp .env.example .env

# Файл уже настроен правильно, ничего менять не нужно!
```

---

### ШАГ 4: Установить AI модели (Ollama)

**Это нужно для распознавания текста в документах.**

```bash
# 1. Запустить Ollama (в отдельном окне терминала)
ollama serve

# 2. В другом окне терминала установить модели:

# Модель для распознавания текста (OCR)
ollama pull deepseek-ocr:latest

# Модель для генерации текста
ollama pull qwen2.5:7b

# Модель для умного поиска
ollama pull nomic-embed-text:latest
```

**Каждая модель весит 2-5 ГБ, установка займёт 10-30 минут в зависимости от интернета.**

---

## ▶️ ЗАПУСК ПРОГРАММЫ

### Вам нужно запустить ТРИ программы одновременно:

#### 1. Ollama (для AI)

**Откройте ПЕРВОЕ окно терминала:**

```bash
ollama serve
```

**Оставьте это окно работать!** Вы увидите:
```
Listening on 127.0.0.1:11434
```

---

#### 2. Backend (сервер)

**Откройте ВТОРОЕ окно терминала:**

```bash
# Перейти в папку backend
cd "Z:\FQ\Документооборот\Jurist\backend"

# Активировать виртуальное окружение
source venv/bin/activate

# Запустить сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Оставьте это окно работать!** Вы увидите:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

**🎉 Сервер запущен!** Можно открыть http://localhost:8000/docs и увидеть список всех API.

---

#### 3. Frontend (веб-интерфейс)

**Откройте ТРЕТЬЕ окно терминала:**

```bash
# Перейти в папку frontend
cd "Z:\FQ\Документооборот\Jurist\frontend"

# Запустить интерфейс
npm run dev
```

**Оставьте это окно работать!** Вы увидите:
```
  VITE v5.x.x  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**🎉 Всё готово!**

---

## 🌐 ОТКРЫТЬ ПРОГРАММУ В БРАУЗЕРЕ

**1. Откройте браузер (Firefox, Chrome, и т.д.)**

**2. Введите адрес:**
```
http://localhost:5173
```

**3. Вы увидите страницу входа.**

**4. Войдите:**
- **Логин:** admin
- **Пароль:** admin123

**5. Вы попадёте на главную страницу (Dashboard)!**

---

## 📖 КАК ПОЛЬЗОВАТЬСЯ ПРОГРАММОЙ

### 1. Создать новое дело

**Шаг 1:** Нажмите кнопку **"Новое дело"** на главной странице

**Шаг 2:** Заполните форму:
- **Номер дела:** например `1/2025`
- **Название дела:** например `Иск о взыскании долга`
- **Тип дела:** выберите из списка (гражданское, уголовное, и т.д.)
- **Статус:** выберите "Новое"
- **Описание:** краткое описание дела

**Шаг 3:** Нажмите **"Сохранить"**

**✅ Дело создано!**

---

### 2. Добавить участника дела (персону)

**Шаг 1:** В меню слева нажмите **"Персоны"**

**Шаг 2:** Нажмите **"Новая персона"**

**Шаг 3:** Заполните форму:
- **ФИО:** например `Иванов Иван Иванович`
- **Тип:** выберите (Клиент, Судья, Адвокат, и т.д.)
- **IDNP/IDNO:** 13 цифр (для молдавских граждан)
- **Телефон:** например `+373 69 123456`
- **Email:** ivan@example.com

**Шаг 4:** Нажмите **"Сохранить"**

**✅ Персона добавлена!**

---

### 3. Загрузить документ

**Шаг 1:** В меню слева нажмите **"Документы"** → **"Загрузить"**

**Шаг 2:** Перетащите файл (PDF, DOCX, изображение) в зону загрузки

**Шаг 3:** Выберите к какому делу относится документ

**Шаг 4:** Поставьте галочку **"Запустить OCR автоматически"** если хотите распознать текст

**Шаг 5:** Нажмите **"Загрузить"**

**✅ Документ загружен!**

**Если включили OCR:**
- Программа автоматически распознает весь текст в документе
- Текст можно будет искать через поиск
- Это займёт 10-30 секунд на страницу

---

### 4. Добавить событие в календарь

**Шаг 1:** В меню слева нажмите **"Календарь"**

**Шаг 2:** Выберите дату на календаре

**Шаг 3:** Нажмите **"Добавить событие"**

**Шаг 4:** Заполните:
- **Тип события:** Судебное заседание, Срок подачи, Встреча
- **Описание:** например `Судебное заседание по делу 1/2025`
- **Дата и время:** выберите

**Шаг 5:** Нажмите **"Сохранить"**

**✅ Событие добавлено!**

---

### 5. Искать информацию

**Вариант 1: Глобальный поиск**
- Вверху страницы есть строка поиска
- Введите что ищете (номер дела, имя, текст)
- Нажмите Enter
- Программа найдёт дела, документы, персоны

**Вариант 2: Поиск по конкретной странице**
- На странице "Дела" есть поле поиска
- Можно фильтровать по статусу
- Можно сортировать по дате

---

### 6. Сгенерировать документ из шаблона

**Шаг 1:** В меню нажмите **"Шаблоны"**

**Шаг 2:** Выберите нужный шаблон (например "Исковое заявление")

**Шаг 3:** Нажмите **"Генерировать"**

**Шаг 4:** Выберите дело, для которого генерировать документ

**Шаг 5:** Нажмите **"Генерировать"**

**✅ Документ скачается автоматически!**

---

## 🔧 ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### ❌ Проблема: "Не могу войти в систему"

**Решение:**
1. Проверьте логин и пароль (admin / admin123)
2. Убедитесь что Backend запущен (окно 2)
3. Проверьте в терминале Backend - нет ли ошибок

---

### ❌ Проблема: "Страница не открывается"

**Решение:**
1. Убедитесь что Frontend запущен (окно 3)
2. Проверьте адрес: должен быть http://localhost:5173
3. Попробуйте другой браузер

---

### ❌ Проблема: "OCR не работает"

**Решение:**
1. Убедитесь что Ollama запущен (окно 1)
2. Проверьте что модель установлена:
   ```bash
   ollama list
   # Должна быть deepseek-ocr
   ```
3. Если модели нет - установите:
   ```bash
   ollama pull deepseek-ocr:latest
   ```

---

### ❌ Проблема: "Ошибка базы данных"

**Решение:**
1. Проверьте что PostgreSQL запущен:
   ```bash
   sudo systemctl status postgresql
   ```
2. Проверьте пароль в файле `backend/.env`
3. Попробуйте заново применить миграции:
   ```bash
   cd backend
   source venv/bin/activate
   alembic upgrade head
   ```

---

### ❌ Проблема: "Не могу загрузить файл больше 50 МБ"

**Решение:**
Это ограничение настроено специально для безопасности.

Если нужно увеличить:
1. Откройте `backend/.env`
2. Найдите строку `MAX_FILE_SIZE=52428800`
3. Измените на нужный размер в байтах (например 104857600 = 100 МБ)
4. Перезапустите Backend

---

## 🛑 КАК ОСТАНОВИТЬ ПРОГРАММУ

**Для остановки нажмите `Ctrl+C` в каждом из трёх окон терминала:**

1. Окно 1 (Ollama) - нажать Ctrl+C
2. Окно 2 (Backend) - нажать Ctrl+C
3. Окно 3 (Frontend) - нажать Ctrl+C

**Все данные сохранятся в базе PostgreSQL!**

---

## 💾 РЕЗЕРВНОЕ КОПИРОВАНИЕ

### Как сделать backup базы данных:

```bash
# Создать backup
pg_dump -U legal_cms_user -d legal_cms > backup_$(date +%Y%m%d).sql

# Файл сохранится как backup_20251210.sql
```

### Как восстановить из backup:

```bash
# Восстановить базу
psql -U legal_cms_user -d legal_cms < backup_20251210.sql
```

**💡 СОВЕТ:** Делайте backup каждую неделю!

---

## 📞 ПОДДЕРЖКА

### Если что-то не работает:

**1. Проверьте логи в терминале:**
- Окно 2 (Backend) - там будут все ошибки сервера
- Окно 3 (Frontend) - там будут ошибки интерфейса

**2. Откройте консоль браузера:**
- Нажмите F12
- Вкладка "Console"
- Посмотрите есть ли красные ошибки

**3. Проверьте что всё запущено:**
```bash
# PostgreSQL
sudo systemctl status postgresql

# Ollama
curl http://localhost:11434/api/tags

# Backend
curl http://localhost:8000/docs

# Frontend
curl http://localhost:5173
```

---

## 🎓 ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ

### Админ панель

**Где:** Меню → "Администрирование" (только для admin)

**Что можно:**
- Создавать новых пользователей
- Смотреть системную информацию
- Делать backup базы данных
- Просматривать журнал аудита (кто что менял)

---

### Поиск по законодательству РМ

**Где:** Меню → "Законодательство"

**Что можно:**
- Искать законы по названию, номеру, дате
- Скачивать PDF законов
- Привязывать законы к делам

---

### Отчёты

**Где:** На странице дела → кнопка "Отчёт"

**Что можно:**
- Сгенерировать карточку дела (PDF)
- Экспортировать список дел (Excel)
- Просмотреть статистику

---

## ✅ ЧЕКЛИСТ ПЕРВОГО ЗАПУСКА

- [ ] PostgreSQL установлен и запущен
- [ ] Python 3.11+ установлен
- [ ] Node.js 18+ установлен
- [ ] Ollama установлен
- [ ] База данных legal_cms создана
- [ ] Backend зависимости установлены (pip install)
- [ ] Frontend зависимости установлены (npm install)
- [ ] Миграции применены (alembic upgrade head)
- [ ] Администратор создан (admin/admin123)
- [ ] AI модели скачаны (ollama pull)
- [ ] Папки storage созданы
- [ ] Все три окна запущены (Ollama, Backend, Frontend)
- [ ] Открыл http://localhost:5173 в браузере
- [ ] Вошёл в систему (admin/admin123)
- [ ] Создал первое дело
- [ ] Загрузил первый документ

**🎉 ПОЗДРАВЛЯЕМ! ВЫ ГОТОВЫ К РАБОТЕ!**

---

**Последнее обновление:** 10.12.2025
**Версия:** 1.0.0
