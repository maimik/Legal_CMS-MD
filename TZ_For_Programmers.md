# ТЕХНИЧЕСКОЕ ЗАДАНИЕ ДЛЯ ПРОГРАММИСТОВ
## Система электронного документооборота юридических дел

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

Разработать **веб-систему для управления юридическими делами** с локальной AI интеграцией.

**Краткое описание:** Замена файловой системы хранения дел (папки с PDF) на полноценную базу данных с веб-интерфейсом, поиском и AI обработкой документов.

---

## 📊 МАСШТАБ ПРОЕКТА

- **Пользователи:** 2 человека (юрист + ассистент)
- **Дела:** ~3-4 новых в неделю, всего до 10,000 дел
- **Документы:** до 100,000 файлов
- **Текущие дела для миграции:** 20 штук

---

## 🛠️ ТЕХНОЛОГИЧЕСКИЙ СТЕК (ОБЯЗАТЕЛЬНЫЙ)

### Backend:
```
Python 3.11+
FastAPI (REST API)
PostgreSQL 15+ (база данных)
SQLAlchemy 2.0 (ORM)
Alembic (миграции БД)
```

### Frontend:
```
Vue.js 3 + Vuetify 3 (рекомендуется)
ИЛИ
Django Templates + Bootstrap 5 (если проще)
```

### AI интеграция:
```
Ollama API (уже установлен на сервере)
- deepseek-ocr:latest - для OCR
- qwen2.5:7b - для генерации текста
- nomic-embed-text:latest - для семантического поиска
```

### Обработка документов:
```
PyPDF2, pdfplumber - работа с PDF
python-docx - генерация DOCX
Pillow - обработка изображений
```

---

## 📁 ОСНОВНЫЕ МОДУЛИ СИСТЕМЫ

### 1. УПРАВЛЕНИЕ ДЕЛАМИ
**Что нужно:**
- CRUD операции для дел
- Поля: номер дела, префикс (LS, CR и т.д.), тип (гражданское/уголовное/административное), истец, ответчик, суд, судья, статус, даты
- Связь дел с персонами (многие-ко-многим)
- Теги и поиск по всем полям
- История изменений

### 2. УПРАВЛЕНИЕ ПЕРСОНАМИ
**Что нужно:**
- CRUD для персон (клиенты, судьи, адвокаты, противники)
- Поля: ФИО, тип, IDNP (молдавский), телефон, email, адрес
- Связь с делами
- Поиск по ФИО, IDNP

### 3. ДОКУМЕНТООБОРОТ ⭐ (КЛЮЧЕВОЙ МОДУЛЬ)
**Что нужно:**
- Загрузка файлов (PDF, DOCX, изображения)
- Автоматический OCR для PDF через Ollama API
- Хранение файлов в `/storage/documents/case_XXX/`
- Метаданные: тип документа, дата, теги, версия
- Полнотекстовый поиск по содержимому (PostgreSQL Full-Text Search)
- Предпросмотр документов в браузере

### 4. ГЕНЕРАЦИЯ ДОКУМЕНТОВ ПО ШАБЛОНАМ
**Что нужно:**
- Загрузка шаблонов (DOCX с переменными типа `{{client_name}}`)
- Подстановка данных из карточки дела
- Опционально: AI генерация текста через Ollama qwen2.5:7b
- Сохранение сгенерированного документа в дело

### 5. КАЛЕНДАРЬ СОБЫТИЙ
**Что нужно:**
- События по делам (заседания, дедлайны)
- Календарное представление (месяц/неделя/день)
- Email напоминания за 1 день и 3 часа до события
- Типы: судебное заседание, срок подачи документа, консультация

### 6. ЗАКОНОДАТЕЛЬНАЯ БАЗА
**Что нужно:**
- Загрузка законов РМ (PDF файлы)
- Полнотекстовый поиск
- Связывание законов с делами
- Категории: законы, кодексы, постановления

### 7. ПОИСК
**Что нужно:**
- Глобальный поиск по всей системе
- Полнотекстовый поиск PostgreSQL (с поддержкой русского языка)
- Семантический поиск через Ollama embeddings (опционально, можно в v2)
- Фильтры по типам, датам, статусам

### 8. ОТЧЁТЫ
**Что нужно:**
- Генерация карточки дела в PDF/DOCX
- Статистика: количество дел по статусам, типам
- Список дел с фильтрами (экспорт в Excel/PDF)

### 9. АДМИНИСТРИРОВАНИЕ
**Что нужно:**
- Авторизация JWT (2 роли: admin, assistant)
- Аудит действий (кто, когда, что изменил)
- Настройки системы (Ollama URL, email SMTP и т.д.)
- Кнопка "Создать резервную копию БД"

---

## 🗄️ СХЕМА БАЗЫ ДАННЫХ

### Основные таблицы:

```sql
-- Пользователи
users (id, username, email, password_hash, role, full_name, is_active)

-- Дела
cases (
    id, case_number, case_prefix, case_type, title, description,
    court, judge, plaintiff, defendant, case_status,
    open_date, close_date, tags[], metadata jsonb
)

-- Персоны
persons (
    id, full_name, person_type, idnp, birth_date,
    phone, email, address_legal, address_actual, notes
)

-- Связь дел и персон
case_persons (id, case_id, person_id, role_in_case)

-- Документы
documents (
    id, case_id, document_type, file_name, file_path,
    file_size, file_format, upload_date, document_date,
    description, ocr_text, tags[], version, is_template
)

-- События
case_events (
    id, case_id, event_type, event_date, description,
    location, reminder_days_before, reminder_sent, event_status
)

-- Законодательная база
legal_acts (
    id, act_type, act_number, act_date, title,
    file_path, tags[], full_text, act_status
)

-- Связь дел и законов
case_legal_acts (id, case_id, legal_act_id)

-- Шаблоны документов
document_templates (
    id, template_name, template_type, file_path, variables jsonb
)

-- Аудит
audit_log (
    id, user_id, action, entity_type, entity_id,
    old_value jsonb, new_value jsonb, created_at
)
```

**📌 Полная SQL схема:** см. файл `database_schema.sql` (приложу отдельно если нужно)

---

## 🔌 ИНТЕГРАЦИЯ С OLLAMA API

### Эндпоинты Ollama (уже запущен на сервере):
```
POST http://localhost:11434/api/generate  - генерация текста
POST http://localhost:11434/api/embeddings - векторы для поиска
GET  http://localhost:11434/api/tags - список моделей
```

### Как использовать:

**OCR документа:**
```python
import httpx

async def ocr_document(file_path: str) -> str:
    # Читаем PDF, конвертируем в base64
    # Отправляем на Ollama
    response = await httpx.post(
        "http://localhost:11434/api/generate",
        json={
            "model": "deepseek-ocr:latest",
            "prompt": f"Распознай текст из этого документа: [base64_image]",
            "stream": False
        },
        timeout=60.0
    )
    return response.json()["response"]
```

**Генерация текста:**
```python
async def generate_text(prompt: str) -> str:
    response = await httpx.post(
        "http://localhost:11434/api/generate",
        json={
            "model": "qwen2.5:7b",
            "prompt": prompt,
            "stream": False
        },
        timeout=30.0
    )
    return response.json()["response"]
```

**Семантический поиск (embeddings):**
```python
async def get_embeddings(text: str) -> list:
    response = await httpx.post(
        "http://localhost:11434/api/embeddings",
        json={
            "model": "nomic-embed-text:latest",
            "prompt": text
        }
    )
    return response.json()["embedding"]
```

---

## 🌐 API СТРУКТУРА

### Основные эндпоинты:

```
POST   /api/auth/login             # Вход
POST   /api/auth/logout            # Выход
GET    /api/auth/me                # Текущий пользователь

GET    /api/cases                  # Список дел (с фильтрами)
POST   /api/cases                  # Создать дело
GET    /api/cases/{id}             # Получить дело
PUT    /api/cases/{id}             # Обновить дело
DELETE /api/cases/{id}             # Удалить дело

GET    /api/persons                # Список персон
POST   /api/persons                # Создать персону
GET    /api/persons/{id}           # Получить персону
PUT    /api/persons/{id}           # Обновить персону
DELETE /api/persons/{id}           # Удалить персону

GET    /api/documents              # Список документов
POST   /api/documents              # Загрузить документ
GET    /api/documents/{id}         # Получить документ
DELETE /api/documents/{id}         # Удалить документ
GET    /api/documents/{id}/download # Скачать файл
POST   /api/documents/{id}/ocr     # Запустить OCR

GET    /api/events                 # Список событий
POST   /api/events                 # Создать событие
GET    /api/events/{id}            # Получить событие
PUT    /api/events/{id}            # Обновить событие
DELETE /api/events/{id}            # Удалить событие

GET    /api/legal-acts             # Список законов
POST   /api/legal-acts             # Загрузить закон
GET    /api/legal-acts/{id}        # Получить закон

GET    /api/templates              # Список шаблонов
POST   /api/templates              # Загрузить шаблон
POST   /api/templates/{id}/generate # Генерация документа

GET    /api/search?q=текст         # Глобальный поиск
POST   /api/search/semantic        # Семантический поиск

GET    /api/reports/case/{id}      # Карточка дела (PDF)
GET    /api/reports/statistics     # Статистика

POST   /api/admin/backup           # Создать бэкап БД
GET    /api/admin/settings         # Настройки системы
PUT    /api/admin/settings         # Обновить настройки
```

---

## 🖥️ ИНТЕРФЕЙС (UI)

### Основные страницы:

**1. Дашборд (главная)**
- Статистика дел по статусам (диаграммы)
- Предстоящие события на неделю
- Последние добавленные дела
- Глобальный поиск

**2. Список дел**
- Таблица с делами (пагинация)
- Фильтры: статус, тип, даты, теги
- Кнопки: добавить, редактировать, удалить
- Клик на дело → переход к карточке

**3. Карточка дела**
Вкладки:
- Общая информация (поля дела)
- Персоны (список участников с ролями)
- Документы (список с превью PDF)
- События (календарь)
- Законодательство (связанные акты)
- История изменений

**4. Документы**
- Список всех документов (с фильтрами)
- Drag-and-drop загрузка
- Предпросмотр PDF в модальном окне
- Кнопка "Запустить OCR" для каждого документа

**5. Календарь**
- Календарное представление (month/week/day)
- Цветовая кодировка по типам событий
- Клик на событие → модальное окно с деталями

**6. Поиск**
- Строка поиска в хедере (всегда видна)
- Страница расширенного поиска с фильтрами
- Результаты с подсветкой совпадений

**7. Настройки**
- Профиль пользователя
- Настройки уведомлений (email)
- Настройки Ollama (URL, модели)
- Резервное копирование
- Управление пользователями (только admin)

---

## 🔐 БЕЗОПАСНОСТЬ

### Обязательно реализовать:

1. **Аутентификация JWT**
   - Access token (15 минут)
   - Refresh token (7 дней)
   - Bcrypt хеширование паролей

2. **Авторизация (RBAC)**
   - Роль `admin`: полный доступ
   - Роль `assistant`: ограниченный доступ (нет удаления дел, нет настроек)

3. **Аудит**
   - Логирование всех действий в таблицу `audit_log`
   - Сохранение старых значений при изменениях

4. **Защита файлов**
   - Проверка типов файлов (magic bytes, не только расширение)
   - Ограничение размера файла (до 50 МБ)
   - Только аутентифицированные пользователи могут скачивать документы

5. **HTTPS**
   - Nginx reverse proxy с SSL сертификатом

---

## 📦 РЕЗЕРВНОЕ КОПИРОВАНИЕ

### Автоматическое:
```bash
# Cron job каждый день в 03:00
0 3 * * * /path/to/backup.sh

# backup.sh:
pg_dump legal_cms | gzip > /path/to/backups/backup_$(date +%Y%m%d_%H%M%S).sql.gz
# Удаление копий старше 30 дней
find /path/to/backups -name "*.sql.gz" -mtime +30 -delete
```

### Ручное:
- Кнопка в админ-панели "Создать резервную копию"
- Скачивание backup файла

---

## 🚀 РАЗВЁРТЫВАНИЕ

### Сервер:
- **ОС:** MX-Linux (Debian-based)
- **Init система:** SysVinit (НЕ systemd)
- **Рабочий стол:** Xfce

### Минимальные требования:
```
CPU: 4 ядра
RAM: 8 ГБ
HDD: 100 ГБ (+ 500 ГБ для документов)
```

### Установленное ПО:
- Python 3.11+
- PostgreSQL 15+
- Nginx
- Ollama (уже установлен, порт 11434)

### Структура директорий:
```
/home/maimik/Projects/legal-cms/
├── backend/              # FastAPI приложение
│   ├── app/
│   ├── venv/
│   └── requirements.txt
├── frontend/             # Vue.js приложение
│   └── dist/             # Скомпилированные файлы
├── storage/              # Хранилище файлов
│   ├── documents/
│   ├── templates/
│   └── legal_acts/
├── backups/              # Резервные копии
│   ├── daily/
│   └── manual/
├── logs/                 # Логи
└── scripts/              # Скрипты обслуживания
    ├── install.sh
    ├── backup.sh
    └── migrate_old_data.py
```

### Доступ:
```
Backend API:  http://192.168.X.X:8000
Frontend:     http://192.168.X.X:8080
Через Nginx:  https://legal-cms.local
```

---

## 📝 ТРЕБОВАНИЯ К КОДУ

### Python (Backend):
- Type hints для всех функций
- Docstrings для классов и функций
- Pydantic модели для валидации
- Async/await для всех DB операций
- Обработка ошибок с понятными сообщениями

### JavaScript (Frontend):
- Vue 3 Composition API
- TypeScript (опционально, но желательно)
- Комментарии для сложной логики
- Переиспользуемые компоненты

### Git:
- Осмысленные commit сообщения
- Feature branches (не коммитить всё в main)

---

## 🧪 ТЕСТИРОВАНИЕ

### Минимум:
- Ручное тестирование всех модулей
- Проверка на тестовых данных (20 дел для миграции)

### Желательно:
- Unit тесты для API endpoints (pytest)
- Тестирование интеграции с Ollama

---

## 📅 СРОКИ И ЭТАПЫ

### ЭТАП 1: Backend базовый (3 недели)
- Настройка проекта FastAPI
- Создание моделей SQLAlchemy
- Миграции Alembic
- CRUD API для дел, персон, документов
- Авторизация JWT

### ЭТАП 2: Backend AI (2 недели)
- Интеграция с Ollama
- OCR сервис
- Полнотекстовый поиск PostgreSQL
- Генерация документов по шаблонам

### ЭТАП 3: Frontend (4 недели)
- Настройка Vue.js + Vuetify
- Все страницы и компоненты
- Интеграция с Backend API
- Календарь событий
- Предпросмотр PDF

### ЭТАП 4: Тестирование (2 недели)
- Исправление багов
- Оптимизация производительности
- Тестирование на реальных данных

### ЭТАП 5: Развёртывание (1 неделя)
- Установка на сервер
- Настройка Nginx
- SSL сертификат
- Миграция 20 дел (через готовый скрипт)
- Обучение пользователей

**ИТОГО: ~12-13 недель (~3 месяца)**

---

## 💰 СТОИМОСТЬ (ОРИЕНТИРОВОЧНО)

**Базовая версия (MVP):**
- Все основные модули
- Без семантического поиска
- Без email уведомлений
- **Стоимость:** 170,000 - 230,000 MDL

**Полная версия:**
- + Семантический поиск
- + Email уведомления
- + Расширенная аналитика
- **Стоимость:** 250,000 - 350,000 MDL

**Поддержка:**
- 10,000 - 15,000 MDL/месяц

---

## 📞 ВАЖНЫЕ КОНТАКТЫ

**Заказчик:** [Юрист]
**Email:** [email]
**Телефон:** [телефон]

**Сервер доступ:**
- SSH: `ssh user@192.168.X.X`
- PostgreSQL: `postgresql://legal_cms_user:password@localhost/legal_cms`
- Ollama: `http://localhost:11434`

---

## 📚 ДОКУМЕНТАЦИЯ

### Что нужно подготовить:
1. **README.md** - описание проекта, установка
2. **API_DOCS.md** - описание всех API endpoints (или Swagger)
3. **DATABASE.md** - схема БД с описанием таблиц
4. **DEPLOYMENT.md** - инструкция по развёртыванию
5. **USER_GUIDE.md** - руководство пользователя (с скриншотами)

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

### ОБЯЗАТЕЛЬНО УЧЕСТЬ:

1. **Локальное размещение** - все данные только на сервере заказчика, никаких облачных сервисов
2. **Молдавская специфика:**
   - IDNP (13-значный код физлиц РМ) - валидация формата
   - IDNO (13-значный код юрлиц РМ) - валидация формата
   - Поддержка румынского языка в полнотекстовом поиске PostgreSQL
3. **Ollama обязательно** - это не опция, AI интеграция критична
4. **Резервное копирование** - автоматическое ежедневно
5. **Производительность:**
   - Загрузка страниц < 2 секунд
   - Поиск < 1 секунда
   - OCR документа < 10 секунд на страницу

---

## 🎯 КРИТЕРИИ ПРИЁМКИ

### Система считается готовой, если:

✅ Все 9 модулей работают  
✅ API endpoints отвечают корректно  
✅ Ollama интеграция работает (OCR + генерация)  
✅ Полнотекстовый поиск находит документы  
✅ Можно загрузить/скачать документы  
✅ Можно создать/отредактировать дело  
✅ Календарь событий отображается  
✅ Генерация карточки дела в PDF работает  
✅ Авторизация и роли работают  
✅ Скрипт миграции успешно перенёс 20 дел  
✅ Автоматический бэкап настроен и работает  
✅ Нет критичных багов  
✅ Документация готова  

---

## 📦 ПОСТАВКА

### Что нужно передать заказчику:

1. **Исходный код** (Git репозиторий)
2. **Скомпилированное приложение** (готовое к запуску)
3. **Скрипт установки** (`install.sh`)
4. **Документация** (все MD файлы)
5. **Доступы:**
   - Логин/пароль администратора
   - Доступ к БД
6. **Обучение:** 2-3 часа демонстрация системы

---

## ❓ ВОПРОСЫ?

Если что-то непонятно:
1. Читайте полное ТЗ: `TZ_Legal_Document_Management_System.md`
2. Смотрите схему БД: раздел "Схема базы данных"
3. Пример интеграции с Ollama: выше в разделе "Интеграция с Ollama API"

**Удачи в разработке! 🚀**

---

**Версия документа:** 1.0  
**Дата:** 08.12.2025  
**Автор:** Legal CMS Team