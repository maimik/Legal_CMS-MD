# ⚡ Быстрый старт Legal CMS-MD

**Шпаргалка для ежедневного запуска**

---

## 🚀 ЗАПУСК (каждый день)

### Откройте 3 окна терминала:

**Окно 1 - Ollama (AI):**
```bash
ollama serve
```

**Окно 2 - Backend (сервер):**
```bash
cd "Z:\FQ\Документооборот\Jurist\backend"
source venv/bin/activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Окно 3 - Frontend (интерфейс):**
```bash
cd "Z:\FQ\Документооборот\Jurist\frontend"
npm run dev
```

### Откройте браузер:
```
http://localhost:5173
```

**Логин:** admin
**Пароль:** admin123

---

## 🛑 ОСТАНОВКА

В каждом окне нажмите: **Ctrl+C**

---

## 💾 BACKUP (каждую неделю)

```bash
pg_dump -U legal_cms_user -d legal_cms > backup_$(date +%Y%m%d).sql
```

---

## 🔧 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

**1. Проверить PostgreSQL:**
```bash
sudo systemctl status postgresql
sudo systemctl start postgresql  # если не запущен
```

**2. Проверить Ollama:**
```bash
curl http://localhost:11434/api/tags
ollama serve  # если не запущен
```

**3. Проверить Backend:**
```bash
curl http://localhost:8000/docs
# Если не работает - смотреть ошибки в окне 2
```

**4. Проверить Frontend:**
```bash
curl http://localhost:5173
# Если не работает - смотреть ошибки в окне 3
```

**5. Перезапустить всё:**
- Нажать Ctrl+C во всех окнах
- Запустить заново (см. ЗАПУСК выше)

---

## 📁 СТРУКТУРА ПАПОК

```
Jurist/
├── backend/          - Сервер (Python)
├── frontend/         - Интерфейс (Vue.js)
├── storage/          - Загруженные файлы
│   ├── documents/    - Документы дел
│   ├── templates/    - Шаблоны DOCX
│   └── legal_acts/   - Законы РМ
└── backups/          - Резервные копии
```

---

## 🔑 ВАЖНЫЕ АДРЕСА

- **Веб-интерфейс:** http://localhost:5173
- **API документация:** http://localhost:8000/docs
- **Админ панель:** http://localhost:5173/admin (только admin)

---

## 📝 ЧАСТЫЕ КОМАНДЫ

**Создать нового пользователя (через консоль):**
```bash
cd backend
source venv/bin/activate
python
>>> from app.utils.security import get_password_hash
>>> print(get_password_hash("новый_пароль"))
# Скопировать хеш и добавить в БД через SQL
```

**Применить новые миграции:**
```bash
cd backend
source venv/bin/activate
alembic upgrade head
```

**Обновить зависимости:**
```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt

cd ../frontend
npm install
```

---

## 🎯 ОСНОВНЫЕ ДЕЙСТВИЯ

**Создать дело:**
Dashboard → "Новое дело" → Заполнить форму → Сохранить

**Добавить документ:**
Меню → Документы → Загрузить → Перетащить файл → Выбрать дело → Загрузить

**Запустить OCR:**
Документы → Выбрать документ → Кнопка "OCR"

**Добавить событие:**
Календарь → Выбрать дату → Добавить событие

**Поиск:**
Строка поиска вверху → Ввести текст → Enter

---

## ⚠️ ВАЖНО ПОМНИТЬ

- **3 окна должны работать одновременно** (Ollama, Backend, Frontend)
- **Не закрывайте окна** пока работаете с программой
- **Делайте backup раз в неделю**
- **OCR работает только если Ollama запущен**
- **Первый вход:** admin / admin123
- **Сменить пароль:** после первого входа в настройках профиля

---

**Полная инструкция:** см. INSTALLATION_GUIDE.md
